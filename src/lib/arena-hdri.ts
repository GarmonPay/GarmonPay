/** Same asset as drei's `preset="studio"` — served from `public/hdri/`. No `@react-three/*` imports (safe for SSR/layout). */
export const ARENA_HDRI_FILE = "studio_small_03_1k.hdr";
export const ARENA_HDRI_PATH = "/hdri/";
