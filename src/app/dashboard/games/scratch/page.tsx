import { FeatureComingSoon } from "@/components/dashboard/FeatureComingSoon";

export default function DashboardScratchCardPage() {
  return (
    <FeatureComingSoon
      emoji="🎫"
      title="Scratch Card"
      description="Scratch cards are on the way. Come back soon to reveal instant rewards."
    />
  );
}
