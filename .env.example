# Copy to .env.local and fill in your values
# Production site URL (auth redirects, referral links). HTTPS enforced in production.
NEXT_PUBLIC_SITE_URL=https://garmonpay.com

# Required for Supabase (dashboard, auth, data, earnings, referrals, ads)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Required for server-side data (dashboard stats, transactions, withdrawals, referrals)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Stripe (payments + Connect payouts). Publishable key is safe for client; secret key is server-only.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxx
STRIPE_SECRET_KEY=sk_live_xxx
# Required for recording payments in DB. In Stripe Dashboard → Developers → Webhooks add: checkout.session.completed, account.updated, customer.subscription.updated, customer.subscription.deleted
STRIPE_WEBHOOK_SECRET=whsec_xxx
