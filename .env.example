# =============================================================================
# PRODUCTION — Copy to .env.local and use REAL values (no demo placeholders).
# Without these, wallet/deposit/funds will not work (app falls back to demo).
# On Vercel: set NEXT_PUBLIC_SITE_URL=https://garmonpay.com so all redirects use GarmonPay.com (not localhost).
# =============================================================================

# Site URL — MUST be https://garmonpay.com in production (Stripe return URLs, auth, referrals)
NEXT_PUBLIC_SITE_URL=https://garmonpay.com

# Supabase — get from Supabase project Settings > API
NEXT_PUBLIC_SUPABASE_URL=YOUR_REAL_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_REAL_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_REAL_KEY

# Stripe — get from Stripe Dashboard > Developers > API keys (use live keys for production)
STRIPE_SECRET_KEY=sk_live_XXXXX
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_XXXXX
# Webhook signing secret: Stripe Dashboard > Developers > Webhooks > your endpoint
STRIPE_WEBHOOK_SECRET=whsec_XXXXX
